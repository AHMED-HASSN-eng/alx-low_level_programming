#include "main.h"
/**
 * _islower - print_alphabet lore
 * @c: the charcter in ASCII code
 * Return: 1 is alphabet is lower else 0
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
